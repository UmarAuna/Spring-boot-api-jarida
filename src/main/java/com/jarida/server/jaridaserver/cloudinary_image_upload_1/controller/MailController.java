package com.jarida.server.jaridaserver.cloudinary_image_upload_1.controller;

/*
@RestController
@RequestMapping("/api/v1")
@Validated
@Api(tags = "Send mail API v1")
@SwaggerDefinition(tags = {
        @Tag(name = "Send mail", description = "This is for Sending mail Api")
})
public class MailController {

    @Autowired
    private MailService mailService;

    @PostMapping("/sendmail")
    public Boolean send(@RequestParam(name = "toEmail") String toEmail,
                        @RequestParam(name = "subject") String subject,
                        @RequestParam(name = "text") String text) {
        return mailService.send(toEmail, subject, text);
    }
}
*/
